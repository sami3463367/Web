# runtime data (SQLite DB, WAL files) — never commit customer data
# (root-anchored: the seed module lives in server/ and MUST stay tracked)
/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# logs
*.log
npm-debug.log*

# env
.env
.env.*
!.env.example

# editor / OS
.DS_Store
.idea/
.vscode/

# dependencies (there are none — keep it that way)
node_modules/
